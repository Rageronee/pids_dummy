services:
  db:
    image: postgres:15-alpine
    container_name: pids-postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD:?DATABASE_PASSWORD env variable is required}
      POSTGRES_DB: eltran_pids
    ports:
      - "5432:5432"
    volumes:
      - pids_db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  adminer:
    image: adminer
    container_name: pids-adminer
    restart: always
    ports:
      - "127.0.0.1:8080:8080"
    depends_on:
      db:
        condition: service_healthy

  # Placeholder for future Device/Sensor Service (e.g. GPSD)
  # gpsd:
  #   image: ghcr.io/mikenye/docker-gpsd
  #   container_name: pids-gpsd
  #   devices:
  #     - "/dev/ttyUSB0:/dev/ttyUSB0"
  #   ports:
  #     - "2947:2947"
  #   restart: always

volumes:
  pids_db_data:

